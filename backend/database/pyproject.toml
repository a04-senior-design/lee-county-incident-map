[project]
name = "lee-county-database"
version = "0.1.0"
description = "Schema migrations for the Lee County backend"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.16",
    "psycopg[binary]>=3.3",
]

[tool.uv]
package = false
